We found another critical crash.

Current problem:
When I open "New Alert", the app crashes immediately and shows:
"Something went wrong"

This means the crash is not limited to Shelter In / Blackout.
It also happens on the New Alert flow.

==================================================
REQUIRED ACTION
==================================================

Do not guess.
Capture the actual runtime error for the New Alert crash.

Please inspect the exact screen/component opened by "New Alert"
and add temporary logging / try-catch around:

1) screen mount
2) initial form state creation
3) selected zone / selected alert type defaults
4) any useMemo / useEffect in the alert screen
5) any map / modal / picker used by New Alert
6) any array access like .map / .filter / .find on possibly undefined values
7) any store selector that assumes alerts/zones/locations are always defined

==================================================
CHECK THESE SPECIFIC LIKELY ISSUES
==================================================

- undefined default zone
- undefined selected alert type
- undefined emergencyModes / alerts / locations / zones arrays
- stale persisted state after migrations
- broken selector returning undefined
- component render crash before user interaction
- modal opening logic using invalid state
- New Alert screen using old assumptions after recent emergency changes

==================================================
IMPORTANT
==================================================

Add real error logging so the exact exception appears in Expo logs.

Do NOT just add more broad fallbacks blindly.
Find the exact runtime error and fix the root cause.

If needed:
- wrap the New Alert screen body in a local error boundary temporarily
- log the failing component and stack

==================================================
EXPECTED RESULT
==================================================

Opening "New Alert" must not crash.
The form/screen should open normally.

==================================================
AFTER FIX
==================================================

Tell me:
1) exact runtime error found
2) exact file and component causing it
3) whether the crash was render-time or action-time
4) what changed to make New Alert stable