Now the real issue is clearer.

Current wrong behavior:
The shelter linking modal is still using the old / previously renamed zone,
instead of detecting which zone the shelter is actually inside right now.

Example:
A zone was renamed from CPF to Camp.
Now the shelter link flow still follows that old renamed zone reference,
instead of checking the shelter's actual current zone position on the map.

==================================================
CORRECT REQUIRED BEHAVIOR
==================================================

When opening "Link Locations" for a shelter:

1) Determine which zone polygon currently contains the shelter coordinates
2) Use that CURRENT zone as the source of truth
3) Show only the locations that belong to that current zone
4) Do NOT rely on stale zone name
5) Do NOT rely on old renamed zone reference
6) Do NOT rely on zones[0]
7) Do NOT rely only on previously saved shelter.zoneName if it became stale

==================================================
SOURCE OF TRUTH
==================================================

Shelter current coordinates on map
→ detect containing zone polygon
→ get current zone.id
→ filter locations by location.zoneId === currentZone.id

If no containing polygon exists:
- fallback to nearest zone center

But first priority must be:
CURRENT polygon containment

==================================================
DEBUG REQUIRED
==================================================

Before fixing, log at runtime:
- shelter id
- shelter name
- shelter coordinates
- saved shelter.zoneId
- saved shelter.zoneName
- detected current containing zone.id
- detected current containing zone.name
- final location list being shown

==================================================
WHAT TO FIX
==================================================

If saved shelter.zoneId or zoneName is stale after zone rename,
then Link Locations must use live zone detection from coordinates,
not stale saved name.

If shelter is moved or zone is renamed,
Link Locations must still show the correct current zone locations.

==================================================
EXPECTED RESULT
==================================================

Place shelter inside a zone
→ open Link Locations
→ system detects actual current zone from map
→ shows correct locations for that zone

Not old name.
Not stale reference.
Not previous zone.

After fixing, tell me:
1) what old stale field caused the bug
2) whether zone rename broke shelter reference
3) what runtime source of truth is now used
4) exact file changed