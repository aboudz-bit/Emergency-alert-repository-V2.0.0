Use Google Maps as the base map only.

Goal:
Replace the current map background/base layer with Google Maps satellite-style base map,
while keeping ALL of our existing overlays and app logic exactly as they are.

CRITICAL RULE:
Google Maps must be used only as the base map.
Do NOT rebuild the app map system from scratch.
Do NOT remove our existing custom map layers.

Must keep working exactly as they are:
- zones
- zone polygons
- locations
- shelters
- warning / hazard zones
- hot / warm / cold circles
- live personnel markers
- selected location highlights
- emergency overlays
- wind indicator
- map click interactions
- admin / eco / supervisor / backup / contractor map behavior

What to change:
1) Replace only the underlying map/base tiles/provider with Google Maps satellite view
2) Remove default Google UI controls:
   - no search bar
   - no Google app UI
   - no default zoom controls
   - no default compass
   - no map toolbar
   - no bottom panel
3) Keep our own custom overlays mounted on top of the map
4) Keep existing store/state logic untouched
5) Keep existing role-based map behavior untouched
6) Keep existing markers, polygons, circles, and interactions untouched

Implementation preference:
- update only map configuration / provider / base layer
- do not rewrite overlay rendering logic unless absolutely necessary
- do not replace our custom map objects with Google-native ones unless needed for compatibility
- preserve current data flow from store → map → overlays

Apply this across ALL app map screens:
- Super Admin
- ECO
- Supervisor
- Backup Supervisor
- Contractor / User
- any shared map screen using common map components

If there are multiple map implementations in the project
(for example GoogleMapsView, LeafletPreviewFallback, shared ZoneMap, etc.),
make sure the base-map change is applied consistently to all relevant screens.

Expected result:
- same app behavior
- same overlays
- same zones / shelters / locations / warning zones
- only the map background becomes clean Google satellite style

Important:
Do NOT break anything.
Before changing code, identify exactly which files control:
- the base map provider
- shared map rendering
- overlay rendering

After implementation, tell me:
1) which files were changed
2) which map implementation(s) were updated
3) how overlays were preserved
4) whether all map screens now use the new base map consistently