We need to upgrade location ordering in the "Zones and Locations" page to true drag-and-drop reordering, and also fix the New Location modal footer layout.

==================================================
1) DRAG-AND-DROP LOCATION REORDERING
==================================================

Requirement:
I want to manually reorder locations inside each zone using drag-and-drop.

Current problem:
Locations are shown in a fixed order.
I need full manual control over the order.

Required behavior:
1) Add drag-and-drop reordering for locations inside each zone
2) User must be able to press and drag a location row up or down
3) Reordering must work only inside the same zone
4) Do NOT move locations between zones during reorder
5) Save the updated order in store/state
6) Order must persist after navigation / reload
7) Rendering must always use the saved custom order

Data requirement:
- Add an order / sortOrder field to locations if not already present
- Sort locations by that field before rendering

UI requirements:
- Add a visible drag handle on each location row
- Keep the existing edit button and active toggle
- Dragging should feel smooth and mobile-friendly
- If drag-and-drop library is too risky, implement a stable manual reorder pattern with move-up / move-down buttons as fallback, but drag-and-drop is preferred

Expected result:
Inside CPF zone, I can reorder:
OT-1
OT-2
OT-3
Gas Train-1
Gas Train-2
CCR
OT-4

into any custom order I want.

==================================================
2) FIX NEW LOCATION MODAL FOOTER
==================================================

Current problem:
When pressing "+" and opening "New Location",
the action buttons at the bottom are badly arranged / cut off on mobile.

Required fix:
1) Fix modal footer layout
2) "Cancel" and "Add" buttons must always be fully visible
3) No clipping
4) No overlap
5) Proper spacing
6) Responsive on small mobile width

Preferred layout:
- two equal-width buttons
- same row if there is enough space
- if not enough space, stack vertically cleanly

==================================================
IMPORTANT
==================================================

Do NOT break:
- rename zone
- merge zones
- move locations between zones
- split zone
- archive zone
- add new zone flow
- existing location edit flow
- map / alerts / shelters / overlays

Only fix:
- drag-and-drop ordering
- stored location order
- modal footer button layout

==================================================
AFTER IMPLEMENTATION
==================================================

Tell me:
1) which files changed
2) how location order is stored
3) how drag-and-drop works
4) what library or approach was used
5) how the New Location modal footer was fixed